"""Blueprint §11: the eight Rishis as reasoning roles.

The existing eight personas keep their names. `agam`, `vyom`, `ritam` and the
rest are voices, annotated across the whole corpus as `rishi_affinity`, and
renaming them would silently change what every one of those annotations means.
What this package adds is a *role* per persona - an analytical remit, the state
it is given, and the condition under which it is invited at all.
"""
